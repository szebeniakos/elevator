export const FLOORS = [0, 1, 2, 3, 4, 5, 6]; // try to add more

export const ELEVATOR_DIRECTIONS = {
  UP: 1,
  DOWN: -1,
  IDLE: 0,
};

export const ELEVATOR_DELAY = 2000; // for proper testing, you can increase the duration (in milliseconds) that an elevator stays on a single floor
